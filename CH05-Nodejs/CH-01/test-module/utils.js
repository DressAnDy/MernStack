// utils là một file dùng để lưu các hàm tiện ích
// Khi nào cần thì có thể dùng lại
let sum = (a,b)  => {
    return a + b; 
}

exports.sumFunction = sum;
// chia sẽ hàm sum trong utils dưới dạng tên là sumFunction cho những thằng o638 ngoài
// 